pipeline {
  agent any
  stages {
    stage('run collection') {
      steps {
        bat 'docker run -t postman/newman run -h'
        bat 'docker run -v /$PWD:/etc/newman --workdir /etc/newman -t postman/newman run collection-1.json --color off --disable-unicode'
      }
    }
  }
}
